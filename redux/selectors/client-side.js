export const isMobile = (state) => state.clientSide.isMobileResolution;

export const isTablet = (state) => state.clientSide.isTabletResolution;

export const isDesktop = (state) => state.clientSide.isDesktopResolution;
